\chapter{Exact Build Times of Launcher App}
\label{app:build_times}

\section{Before New Dependency System}
The run targets are: \code{check connectedCheck assembleRelease}. The time of all tasks can be seen below. Notice that the emulator time for both the fast and slow are included, with the slow in parentheses.

%\begin{tabular}{l|c}%
%    \bfseries Task & \bfseries Milliseconds
%    \csvreader[separator=semicolon, respect all=true]{olddependencysystem.csv}{}% use head of csv as column names
%    {\\\hline\Task & \Milliseconds}% specify your coloumns here
%\end{tabular}

\pgfplotstabletypeset[
    font=\footnotesize,
    begin table=\begin{longtable},
    end table=\end{longtable},
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule\caption{Running time of tasks of old dependency system}},
    col sep=semicolon,
    %monotext/.style={%
    %    postproc cell content/.append style={/pgfplots/table/@cell content/.add={\mono{}{}}},
    %}
    columns/Task/.style={
        string type,
        column type=l%,
        %postproc cell content/.append style={
        %    /pgfplots/table/@cell content/.add={\}{\EUR{}},
    },%},
    columns/Milliseconds/.style={string type, column type=r},
    %columns/Task/.append style={monotext},
    columns={Milliseconds,Task}, 
   ]{part_appendix/olddependencysystem.csv}

\section{After New Dependency System}
The build time of the Launcher app with the targets \code{check connectedCheck assembleRelease} can be seen in in the first table below. The build time with the targets \code{clean check connectedCheck increaseVersionCode assembleRelease publishApkRelease} can be seen in the second table below.

% Limited targets
\pgfplotstabletypeset[
    font=\footnotesize,
    begin table=\begin{longtable},
    end table=\end{longtable},
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule\caption{Running time of tasks of new dependency system with limited targets}},
    col sep=semicolon,
    %monotext/.style={%
    %    postproc cell content/.append style={/pgfplots/table/@cell content/.add={\mono{}{}}},
    %}
    columns/Task/.style={
        string type,
        column type=l%,
        %postproc cell content/.append style={
        %    /pgfplots/table/@cell content/.add={\}{\EUR{}},
    },%},
    columns/Milliseconds/.style={string type, column type=r},
    %columns/Task/.append style={monotext},
    columns={Milliseconds,Task}, 
   ]{part_appendix/newdependencysystem.csv}

% Full targets
\pgfplotstabletypeset[
    font=\footnotesize,
    begin table=\begin{longtable},
    end table=\end{longtable},
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule\caption{Running time of tasks of new dependency system with full targets}},
    col sep=semicolon,
    %monotext/.style={%
    %    postproc cell content/.append style={/pgfplots/table/@cell content/.add={\mono{}{}}},
    %}
    columns/Task/.style={
        string type,
        column type=l%,
        %postproc cell content/.append style={
        %    /pgfplots/table/@cell content/.add={\}{\EUR{}},
    },%},
    columns/Milliseconds/.style={string type, column type=r},
    %columns/Task/.append style={monotext},
    columns={Milliseconds,Task}, 
   ]{part_appendix/newdependencysystem_fulltargets.csv}

\section{Before Tablets}
The build time of the Launcher app before using tablets can be seen in in the first table below.

\pgfplotstabletypeset[
    font=\footnotesize,
    begin table=\begin{longtable},
    end table=\end{longtable},
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule\caption{Running time of tasks before tablet usage}},
    col sep=semicolon,
    %monotext/.style={%
    %    postproc cell content/.append style={/pgfplots/table/@cell content/.add={\mono{}{}}},
    %}
    columns/Task/.style={
        string type,
        column type=l%,
        %postproc cell content/.append style={
        %    /pgfplots/table/@cell content/.add={\}{\EUR{}},
    },%},
    columns/Milliseconds/.style={string type, column type=r},
    %columns/Task/.append style={monotext},
    columns={Milliseconds,Task}, 
   ]{part_appendix/emulator.csv}

\section{After Tablets}
The build time of the Launcher app after using tablets can be seen in in the first table below.

\pgfplotstabletypeset[
    font=\footnotesize,
    begin table=\begin{longtable},
    end table=\end{longtable},
    every head row/.style={before row=\toprule,after row=\midrule},
    every last row/.style={after row=\bottomrule\caption{Running time of tasks after tablet usage}},
    col sep=semicolon,
    %monotext/.style={%
    %    postproc cell content/.append style={/pgfplots/table/@cell content/.add={\mono{}{}}},
    %}
    columns/Task/.style={
        string type,
        column type=l%,
        %postproc cell content/.append style={
        %    /pgfplots/table/@cell content/.add={\}{\EUR{}},
    },%},
    columns/Milliseconds/.style={string type, column type=r},
    %columns/Task/.append style={monotext},
    columns={Milliseconds,Task}, 
   ]{part_appendix/tablet.csv}